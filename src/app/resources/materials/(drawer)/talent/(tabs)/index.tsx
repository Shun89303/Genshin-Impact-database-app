import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function TalentMats() {
	return (
		<SafeAreaView>
			<Text>Talent Materials</Text>
		</SafeAreaView>
	);
}
