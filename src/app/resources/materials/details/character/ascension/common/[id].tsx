import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function CommonAscensionDetails() {
	return (
		<SafeAreaView>
			<Text>Common Ascension Materials</Text>
		</SafeAreaView>
	);
}
